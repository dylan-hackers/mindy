$CPPFLAGS = '-I$(SRCDIR)';

if ($win32) {
    $binary_name = 'parsergen.exe';
} else {
    $binary_name = 'parsergen';
} 
   
push(@compile_dependencies, $binary_name);
push(@files_to_clean, $binary_name);

do emit_library_rule('parsergen', '$(BUILDROOT)/force.timestamp', '');
do install ('bin', $binary_name);
