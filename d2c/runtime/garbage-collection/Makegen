if ($host_platform{'integer_length'} >= 64) {
        $CFLAGS = "$CFLAGS -DDYLAN_64BIT";
}       
	
$D2CFLAGS = '-L../dylan -L../melange';

do emit_library_rule(
    'garbage-collection', '$(BUILDROOT)/force.timestamp', '', 'compile', 'install'
);
