$D2CFLAGS         # added by update-libdirs
    = $d2c_runtime
    . ' -L../../common/transcendental'
    . ' -L../../d2c/runtime/random';

do emit_library_rule(
    'craps', '$(BUILDROOT)/force.timestamp', '', 'compile', 'compile-mindy',
);
