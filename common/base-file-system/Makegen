&makegen_include("../common-Makegen");

$D2CFLAGS = "$D2CFLAGS -L../standard-io -L../format-out -L../regular-expressions -L../string-ext -L../table-ext -L../collection-extensions -L../streams -L.";

&emit_library_rule('base-file-system', '$(BUILDROOT)/force.timestamp',
		   '', 'compile', 'compile-mindy', 'install');

