
$stage2 || ($D2C = 'mindy -f $(BUILDROOT)/compiler/main/d2c.dbc');
$D2CFLAGS = '-L../dylan -L../streams -L../print -L../melange';
$CPPFLAGS = '-I$(SRCROOT)/runtime';

do emit_library_rule(
    'Format', '$(BUILDROOT)/force.timestamp', '', 'compile', 'install'
);
