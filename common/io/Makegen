&makegen_include("../common-Makegen");

$D2CFLAGS = $D2CFLAGS . ' -L../streams -L../print -L../format'
    . ' -L../standard-io -L../format-out' ;

do emit_library_rule('io', '$(BUILDROOT)/force.timestamp', '',
		     'compile', 'compile-mindy', 'install')
