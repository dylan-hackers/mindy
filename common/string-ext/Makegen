&makegen_include("../common-Makegen");

$D2CFLAGS = $D2CFLAGS . ' -L../coll-ext -L../table-ext';

do emit_library_rule(
    'StringExt', '$(BUILDROOT)/force.timestamp', '', 'compile', 
    'compile-mindy', 'install'
);
