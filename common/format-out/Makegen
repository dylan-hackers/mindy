&makegen_include("../common-Makegen");

$D2CFLAGS = $D2CFLAGS . ' -L../streams -L../print -L../format -L../standard-io';

do emit_library_rule(
    'FormatOut', '$(BUILDROOT)/force.timestamp', '', 'compile',
    'compile-mindy', 'install'
);
