&makegen_include("../common-Makegen");

$D2CFLAGS = $D2CFLAGS . ' -L../../common/common-dylan -L../testworks';

do emit_library_rule(
    'testworks-specs', '$(BUILDROOT)/force.timestamp', '', 'compile', 'install');
