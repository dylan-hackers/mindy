language: c

compiler:
  - gcc
  - clang

addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake
      - ninja-build

script:
  - mkdir build
  - cd build
  - cmake .. -G "Ninja"
  - ninja
  - ninja test

branches:
  only:
    - master
