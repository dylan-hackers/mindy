PROJECT(dbclink C)

SET(dbclink_SOURCES
    dbclink.c
)
ADD_EXECUTABLE(dbclink
               ${dbclink_SOURCES})
SET_TARGET_PROPERTIES(dbclink PROPERTIES VERSION ${MINDY_VERSION})

IF (NOT WIN32)
  INSTALL(TARGETS dbclink DESTINATION bin)
ENDIF()
